---
name: V1.0 No Optional Fields
on:
  pull_request:
    types: [opened, synchronize]
---

# V1.0 Agent Without Optional Fields

This agent has only required fields: name and trigger.

No permissions, no outputs, no allowed-actors, no context, no audit.
This should still parse successfully with defaults.
