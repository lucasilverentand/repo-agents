{
  "name": "@repo-agents/cli",
  "version": "1.1.1",
  "description": "GitHub CLI extension for creating AI-powered GitHub Actions workflows from markdown agent definitions",
  "type": "module",
  "bin": {
    "repo-agents": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "cli": "bun packages/cli/src/index.ts",
    "build": "bun build packages/cli/src/index.ts --outdir dist --target node --minify --packages=bundle",
    "test": "bun test packages/*/src/**/*.test.ts",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "format": "bun x prettier --write \"packages/*/src/**/*.ts\"",
    "typecheck": "bun x tsc --noEmit",
    "docs:dev": "bun run --filter docs dev",
    "docs:build": "bun run --filter docs build",
    "docs:preview": "bun run --filter docs preview",
    "prepublishOnly": "bun run build && bun run test"
  },
  "keywords": [
    "github",
    "cli",
    "claude",
    "ai",
    "automation",
    "workflows",
    "github-actions"
  ],
  "author": "Luca Silverentand",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lucasilverentand/repo-agents.git"
  },
  "homepage": "https://github.com/lucasilverentand/repo-agents#readme",
  "bugs": {
    "url": "https://github.com/lucasilverentand/repo-agents/issues"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "prettier": "^3.3.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
